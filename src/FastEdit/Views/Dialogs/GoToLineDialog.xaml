<Window x:Class="FastEdit.Views.GoToLineDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Go To Line"
        Width="300"
        Height="150"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource WindowForegroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Line number:" Margin="0,0,0,8"/>

        <TextBox Grid.Row="1"
                 x:Name="LineNumberBox"
                 Padding="6,4"
                 Margin="0,0,0,12"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Go" Click="Go_Click" IsDefault="True" Padding="20,6" Margin="0,0,8,0"/>
            <Button Content="Cancel" Click="Cancel_Click" IsCancel="True" Padding="20,6"/>
        </StackPanel>
    </Grid>
</Window>
